# usadas para items que no van a cambiar en la vida el programa
# valores que no pueden ser cambiados se conocen como inmutables
# una lista inmutable es una tuple 

#defining a tuple 
# insted of [] we use () 
# se puede acceder a los elementos de una tuple con el índice 

dimensions = (200, 50)
print(dimensions[0])
print(dimensions[1])

# changing tuple elements, it is not possible 
# dimensions[0] = 23
# print(dimensions[0])